def function(first : int = 0, second : int = 0) -> int:
    """_summary_

    Args:
        first (int, optional): _description_. Defaults to 0.
        second (int, optional): _description_. Defaults to 0.

    Returns:
        int: _description_
    """
    return first + second

function(1,2)